# Copy this file to .env and fill in your Garmin Connect credentials.
# After the first successful login, tokens are cached in .garminconnect/
# and the password is no longer needed until the tokens expire (~1 year).
GARMIN_EMAIL=you@example.com
GARMIN_PASSWORD=your-password
